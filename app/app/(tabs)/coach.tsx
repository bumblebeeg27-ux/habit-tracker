import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function CoachScreen() {
  return (
    <ScreenPlaceholder
      title="Coach"
      subtitle="Ask your AI trainer anything about training, form, or recovery."
    />
  );
}
