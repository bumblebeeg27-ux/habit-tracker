import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function TodayScreen() {
  return (
    <ScreenPlaceholder
      title="Today"
      subtitle="Your workout, streak, and check-in will show up here."
    />
  );
}
