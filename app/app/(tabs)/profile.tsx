import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Profile"
      subtitle="Your goals, stats, and settings will show up here."
    />
  );
}
